#version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: repair_tracker_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=repair_tracker
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=P@$$W0rd2025
    ports:
      - "5333:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d repair_tracker"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  web:
    build: .
    container_name: repair_tracker_web
    command: gunicorn --bind 0.0.0.0:8000 --workers 3 --timeout 120 config.wsgi:application
    volumes:
      - .:/app
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    ports:
      - "8088:8000"
    environment:
      - DEBUG=False
      - SECRET_KEY=npck&e0hxnb6*nml^plrf30_ri(5a!vp-v-7&ktckpos-t0zwl
      - DATABASE_URL=postgresql://admin:P@$$W0rd2025@db:5432/repair_tracker
      - ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
      - DJANGO_SETTINGS_MODULE=config.settings
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    container_name: repair_tracker_nginx
    ports:
      - "88:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - static_volume:/app/staticfiles:ro
      - media_volume:/app/media:ro
    depends_on:
      - web
    restart: unless-stopped

volumes:
  postgres_data:
  static_volume:
  media_volume:
